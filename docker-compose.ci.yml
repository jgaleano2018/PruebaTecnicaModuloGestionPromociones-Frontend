services:

  sqlserver:
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Developer"

  backend:
    environment:
      NODE_ENV: production

  frontend:
    build:
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
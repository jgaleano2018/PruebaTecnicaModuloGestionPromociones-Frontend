import { useEffect, useState } from 'react'
import { BarChart3, CalendarDays, Check, ChevronRight, Database, Edit3, LayoutDashboard, Menu, Package, Percent, Plus, RefreshCw, Tag, Trash2, X } from 'lucide-react'
import { demoPromotions, demoStates, demoTypes } from './services/api'
import { promocionService as promotionService } from './infrastructure/services'
import { validatePromotion } from './domain/validation'

const today = new Date().toISOString().slice(0, 10)
const emptyForm = { nombre: '', descripcion: '', tipoDescuentoId: 1, valorDescuento: '', cantidadMinima: '', cantidadPagada: '', fechaInicio: `${today}T09:00`, fechaFin: `${today}T23:59`, activa: false, estadoPromocionId: 1 }

function useObservable(factory, fallback, deps = []) {
  const [value, setValue] = useState(fallback)
  useEffect(() => { const subscription = factory().subscribe({ next: async (response) => { if (!response) return; const data = typeof response.json === 'function' ? await response.json() : response; if (data) setValue(data) }, error: () => setValue(fallback) }); return () => subscription.unsubscribe() }, deps)
  return value
}

function App() {
  const [page, setPage] = useState('promotions'); const [menuOpen, setMenuOpen] = useState(true); const [modal, setModal] = useState(null); const [notice, setNotice] = useState('')
  const [promotions, setPromotions] = useState(demoPromotions)
  const types = useObservable(() => promotionService.types(), demoTypes, [])
  const states = useObservable(() => promotionService.states(), demoStates, [])
  const reload = () => { promotionService.list().subscribe({ next: async (response) => { if (response) { const data = typeof response.json === 'function' ? await response.json() : response; if (Array.isArray(data)) setPromotions(data) } }, error: () => notify('No fue posible cargar las promociones') }) }
  useEffect(() => { reload() }, [])
  const notify = (message) => { setNotice(message); setTimeout(() => setNotice(''), 3500) }
  const save = (form, editing) => { const payload = { ...form, valorDescuento: Number(form.valorDescuento), cantidadMinima: form.cantidadMinima ? Number(form.cantidadMinima) : null, cantidadPagada: form.cantidadPagada ? Number(form.cantidadPagada) : null }; const service = editing ? promotionService.updateState(editing.id, payload) : promotionService.create(payload); service.subscribe(() => { if (!editing) setPromotions((items) => [...items, { ...payload, id: Math.max(...items.map((item) => item.id), 0) + 1, tipoDescuentoNombre: types.find((type) => type.id === Number(payload.tipoDescuentoId))?.nombre || 'Porcentaje', estadoPromocionNombre: states.find((state) => state.id === Number(payload.estadoPromocionId))?.nombre || 'Programada' }]); else setPromotions((items) => items.map((item) => item.id === editing.id ? { ...item, ...payload } : item)); setModal(null); notify(editing ? 'Promoción actualizada' : 'Promoción creada') }) }
  const remove = (promotion) => { if (!window.confirm(`¿Eliminar “${promotion.nombre}”?`)) return; promotionService.remove(promotion.id).subscribe(() => { setPromotions((items) => items.filter((item) => item.id !== promotion.id)); notify('Promoción eliminada') }) }
  return <div className="app-shell"><aside className={`sidebar ${menuOpen ? '' : 'collapsed'}`}><div className="brand"><div className="brand-mark"><Tag size={20} /></div>{menuOpen && <div><strong>Mercado Norte</strong><small>Operaciones</small></div>}</div><nav><button className={page === 'promotions' ? 'active' : ''} onClick={() => setPage('promotions')}><LayoutDashboard size={18} />{menuOpen && 'Gestión de promociones'}</button><button className={page === 'summary' ? 'active' : ''} onClick={() => setPage('summary')}><BarChart3 size={18} />{menuOpen && 'Vista de resumen'}</button></nav><div className="sidebar-foot">{menuOpen && <><span className="status-dot" /> API conectada</>}<button className="icon-button" aria-label="Contraer menú" onClick={() => setMenuOpen(!menuOpen)}><Menu size={19} /></button></div></aside><main className="content"><header className="topbar"><div><span className="eyebrow">CENTRO DE CONTROL / {page === 'promotions' ? 'PROMOCIONES' : 'RESUMEN'}</span><h1>{page === 'promotions' ? 'Gestión de promociones' : 'Vista de resumen'}</h1></div><div className="top-actions"><span className="date-chip"><CalendarDays size={15} /> 27 ago 2026</span><button className="avatar">AM</button></div></header>{page === 'promotions' ? <PromotionPage promotions={promotions} onAdd={() => setModal({ type: 'form', data: null })} onEdit={(item) => setModal({ type: 'form', data: item })} onRemove={remove} onReload={reload} /> : <SummaryPage promotions={promotions} notify={notify} />} {notice && <div className="toast"><Check size={17} />{notice}</div>}</main>{modal?.type === 'form' && <PromotionModal data={modal.data} types={types} states={states} onClose={() => setModal(null)} onSave={save} />}</div>
}

function PromotionPage({ promotions, onAdd, onEdit, onRemove, onReload }) { return <section className="page-body"><div className="section-heading"><div><p className="section-kicker">CATÁLOGO COMERCIAL</p><h2>Promociones activas y programadas</h2><p className="muted">Administra el calendario de beneficios para tus clientes.</p></div><div className="heading-actions"><button className="button secondary" onClick={onReload}><RefreshCw size={16} />Actualizar</button><button className="button primary" onClick={onAdd}><Plus size={17} />Adicionar</button></div></div><div className="metrics"><Metric icon={<Tag />} label="Total promociones" value={promotions.length} tone="blue" /><Metric icon={<Check />} label="Activas" value={promotions.filter((item) => item.estadoPromocionNombre === 'Activa').length} tone="green" /><Metric icon={<CalendarDays />} label="Programadas" value={promotions.filter((item) => item.estadoPromocionNombre === 'Programada').length} tone="orange" /></div><div className="table-card"><div className="table-head"><div><h3>Listado de promociones</h3><span className="muted">{promotions.length} registros encontrados</span></div><span className="table-label"><Database size={14} /> Datos en tiempo real</span></div><div className="table-wrap"><table><thead><tr><th>Promoción</th><th>Tipo</th><th>Valor</th><th>Vigencia</th><th>Estado</th><th className="align-right">Acciones</th></tr></thead><tbody>{promotions.map((item) => <tr key={item.id}><td><strong>{item.nombre}</strong><small>{item.descripcion || 'Sin descripción'}</small></td><td>{item.tipoDescuentoNombre}</td><td><strong>{item.tipoDescuentoNombre === 'Porcentaje' ? `${item.valorDescuento}%` : item.valorDescuento}</strong></td><td><span>{formatDate(item.fechaInicio)}</span><small>hasta {formatDate(item.fechaFin)}</small></td><td><span className={`badge ${item.estadoPromocionNombre.toLowerCase()}`}>{item.estadoPromocionNombre}</span></td><td className="align-right"><button className="table-action edit" aria-label={`Editar ${item.nombre}`} onClick={() => onEdit(item)}><Edit3 size={16} /></button><button className="table-action delete" aria-label={`Eliminar ${item.nombre}`} onClick={() => onRemove(item)}><Trash2 size={16} /></button></td></tr>)}</tbody></table></div></div></section> }
function Metric({ icon, label, value, tone }) { return <div className="metric"><div className={`metric-icon ${tone}`}>{icon}</div><div><span>{label}</span><strong>{value.toString().padStart(2, '0')}</strong></div></div> }
function formatDate(value) { return new Intl.DateTimeFormat('es-CO', { day: '2-digit', month: 'short', year: 'numeric' }).format(new Date(value)).replace('.', '') }

function PromotionModal({ data, types, states, onClose, onSave }) { const [form, setForm] = useState(data ? { ...data, fechaInicio: data.fechaInicio.slice(0, 16), fechaFin: data.fechaFin.slice(0, 16) } : emptyForm); const [errors, setErrors] = useState({}); const change = (event) => setForm({ ...form, [event.target.name]: event.target.type === 'checkbox' ? event.target.checked : event.target.value }); const submit = (event) => { event.preventDefault(); const next = validatePromotion(form); setErrors(next); if (!Object.keys(next).length) onSave(form, data) }; return <div className="modal-backdrop"><div className="modal"><div className="modal-header"><div><span className="eyebrow">MÓDULO GESTIÓN DE PROMOCIONES</span><h2>{data ? 'Editar promoción' : 'Adicionar promoción'}</h2></div><button className="icon-button" aria-label="Cerrar" onClick={onClose}><X size={20} /></button></div><form onSubmit={submit}><div className="form-grid"><Field label="Nombre *" name="nombre" value={form.nombre} onChange={change} maxLength={150} error={errors.nombre} /><Field label="Descripción" name="descripcion" value={form.descripcion} onChange={change} wide /><Select label="Tipo de descuento" name="tipoDescuentoId" value={form.tipoDescuentoId} options={types} onChange={change} /><Field label="Valor descuento" name="valorDescuento" type="number" step="0.01" value={form.valorDescuento} onChange={change} /><Field label="Cantidad mínima" name="cantidadMinima" type="number" step="1" value={form.cantidadMinima} onChange={change} /><Field label="Cantidad pagada" name="cantidadPagada" type="number" step="1" value={form.cantidadPagada} onChange={change} /><Field label="Fecha inicio *" name="fechaInicio" type="datetime-local" value={form.fechaInicio} onChange={change} error={errors.fechaInicio} /><Field label="Fecha fin *" name="fechaFin" type="datetime-local" value={form.fechaFin} onChange={change} error={errors.fechaFin} /><Select label="Estado de promoción" name="estadoPromocionId" value={form.estadoPromocionId} options={states} onChange={change} /><label className="toggle-field"><span>Estado activo</span><input type="checkbox" name="activa" checked={form.activa} onChange={change} /><i /></label></div><div className="modal-footer"><button type="button" className="button secondary" onClick={onClose}>Cancelar</button><button type="submit" className="button primary"><Check size={16} />Guardar</button></div></form></div></div> }
function Field({ label, name, type = 'text', value, onChange, error, wide, ...props }) { return <label className={`field ${wide ? 'wide' : ''}`}><span>{label}</span><input name={name} type={type} value={value ?? ''} onChange={onChange} {...props} />{error && <em>{error}</em>}</label> }
function Select({ label, name, value, options, onChange }) { return <label className="field"><span>{label}</span><select name={name} value={value} onChange={onChange}>{options.map((option) => <option key={option.id} value={option.id}>{option.nombre}</option>)}</select></label> }

function SummaryPage({ promotions, notify }) { const [from, setFrom] = useState('2026-08-01'); const [to, setTo] = useState('2026-08-31'); const counts = { programada: promotions.filter((item) => item.estadoPromocionNombre === 'Programada').length, activa: promotions.filter((item) => item.estadoPromocionNombre === 'Activa').length, finalizada: promotions.filter((item) => item.estadoPromocionNombre === 'Finalizada').length, total: promotions.length }; const current = promotions.filter((item) => item.fechaInicio.slice(0, 10) <= to && item.fechaFin.slice(0, 10) >= from && item.estadoPromocionNombre !== 'Finalizada'); return <section className="page-body"><div className="section-heading"><div><p className="section-kicker">LECTURA EJECUTIVA</p><h2>Resumen de operación</h2><p className="muted">Una lectura rápida del estado actual de tus promociones.</p></div><button className="button secondary" onClick={() => notify('Sesión cerrada correctamente')}><ChevronRight size={16} />Salir</button></div><div className="summary-grid"><div className="summary-panel"><div className="panel-title"><div><span className="section-kicker">CONTADOR SIMPLE POR ESTADO</span><h3>Distribución de promociones</h3></div><BarChart3 size={20} /></div><div className="count-grid"><Count label="Programadas" value={counts.programada} tone="orange" /><Count label="Activas" value={counts.activa} tone="green" /><Count label="Finalizadas" value={counts.finalizada} tone="gray" /><Count label="Total" value={counts.total} tone="blue" /></div></div><div className="summary-panel accent-panel"><div className="panel-title"><div><span className="section-kicker">INDICADOR</span><h3>Cobertura vigente</h3></div><Percent size={20} /></div><strong className="big-number">{counts.total ? Math.round((counts.activa / counts.total) * 100) : 0}<small>%</small></strong><p className="muted">de tus promociones están activas hoy</p></div></div><div className="table-card"><div className="table-head"><div><span className="section-kicker">PROMOCIONES VIGENTES</span><h3>Consulta por rango de fechas</h3></div><span className="table-label"><Package size={14} />{current.length} vigentes</span></div><div className="filters"><label className="field"><span>Fecha inicio</span><input type="date" value={from} onChange={(event) => setFrom(event.target.value)} /></label><label className="field"><span>Fecha fin</span><input type="date" value={to} onChange={(event) => setTo(event.target.value)} /></label><button className="button primary filter-button" onClick={() => notify(`Consulta actualizada: ${current.length} promociones`)}><RefreshCw size={16} />Consultar</button></div><div className="current-list">{current.length ? current.map((item) => <div className="current-row" key={item.id}><div className="current-icon"><Tag size={17} /></div><div><strong>{item.nombre}</strong><span>{item.descripcion || 'Promoción comercial'}</span></div><span className={`badge ${item.estadoPromocionNombre.toLowerCase()}`}>{item.estadoPromocionNombre}</span><strong className="current-value">{item.tipoDescuentoNombre === 'Porcentaje' ? `${item.valorDescuento}%` : item.tipoDescuentoNombre}</strong></div>) : <div className="empty">No hay promociones vigentes en este rango.</div>}</div></div></section> }
function Count({ label, value, tone }) { return <div className="count"><span className={`count-dot ${tone}`} /><span>{label}</span><strong>{value}</strong></div> }

export default App
